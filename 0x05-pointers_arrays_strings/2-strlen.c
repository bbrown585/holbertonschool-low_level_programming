#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - string length
 * @s: idk
 * Return:0
 */
int _strlen(char *s)
{
	int length = 0;

	while
(s[length] != '\0') length++);
	return (length);
}
