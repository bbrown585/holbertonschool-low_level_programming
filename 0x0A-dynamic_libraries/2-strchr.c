#include "main.h"
#include <stdio.h>

/**
 * *_strchr - find strings
 * @s: array
 * @c: not sure
 * Return: 0.
 */
char *_strchr(char *s, char c)
{
	while
	(*s++)
	if (*s == c)
		return (s);
	{
	return ('\0');
	}
}
